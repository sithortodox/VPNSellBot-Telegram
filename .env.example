# .env.example — шаблон переменных окружения ReputMail Bot

# -------------------------------------------------------

# Скопируйте этот файл как ".env" и замените значения.

# Оставьте кавычки, только если в строке встречаются пробелы.

# --- Telegram ---------------------------------------------------

BOT_TOKEN="123456\:ABC-DEF1234ghIkl-zyx57W2v1u123ew11"   # токен из BotFather
ADMIN_IDS="123456789,987654321"                         # список TG‑ID админов (через запятую)

# --- Database ---------------------------------------------------

# Для продакшена используйте PostgreSQL, пример:

# postgresql+asyncpg://USER\:PASSWORD\@host\:port/dbname

# Для локальной SQLite оставьте закомментированным (будет data.db)

# DATABASE\_URL="postgresql+asyncpg://reputmail\:reputmail\@db/reputmail"

# --- Опционально ------------------------------------------------

DEBUG="true"                   # true/false — verbose логи
LOG\_DIR="logs"                 # куда писать bot.log внутри контейнера/хоста

# WEBHOOK\_URL="[https://example.com/bot](https://example.com/bot)"  # если нужен режим webhook
